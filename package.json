{
  "name": "property-manager",
  "private": true,
  "version": "0.0.1",
  "main": "app.js",
  "type": "module",
  "scripts": {
    "install:all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run client\" \"npm run server\"",
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix server",
    "build": "npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix client && npm install --prefix server && npm run prisma:generate --prefix server",
    "start": "node app.js"
  },
  "dependencies": {
    "axios": "^1.16.0",
    "bcryptjs": "^3.0.3",
    "concurrently": "^9.2.1",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "google-auth-library": "^10.6.2",
    "jsonwebtoken": "^9.0.3",
    "nodemailer": "^8.0.7",
    "socket.io": "^4.8.3"
  }
}
